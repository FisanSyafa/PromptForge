version: '3.8'

services:
  db:
    image: postgres:15
    environment:
      POSTGRES_USER: promptforge
      POSTGRES_PASSWORD: berijalan
      POSTGRES_DB: promptforge_db
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
