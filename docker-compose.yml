version: '3.8'

services:
  db:
    image: postgres:15
    env_file:
      - backend/.env
    ports:
      - "${POSTGRES_PORT:-5433}:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
